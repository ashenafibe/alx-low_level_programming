include "main.h"
/**
 * _isdigit - the function n is digit or no
 * @c: argument of the function
 *
 * Return: 1  if it id digit 0 for not
 */
int _isdigit(int c)
 {
	 if (c >= '0' && c <= '9')
	 {
		 return (1);
	 }
	 return (0);
	 
 }
