#include <stdio.h>
/**
 * *main - print the string in the put function
 * *
 * *Description :using the main function
 * Return:0
 */
int main(void)
{      
	char c;
	int i;
	long li;
	long long lli;
	float f;

	printf("The size of char:%ld byte(s)\n,sizeof(c)");
	printf("The size of int,:%ld byte(s)\n,sizeof(i)");
	printf("The size of long:%ld byte(s)\n,sizeof(li)");
        printf("The size of long long:%ld byte(s)\n,sizeof(lli)"); 	
	print("The size of float:%ld byte(s)\n,sizeof(f)");
	return (0);
}
